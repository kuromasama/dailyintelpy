name: 📈 Morning Forecast
on:
  schedule:
   - cron: '45 0 * * *' # 台灣 08:45
  #- cron: '20 4 * * *' # 台灣 12:05 (測試用)
  workflow_dispatch:
jobs:
  run_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with: { python-version: '3.10' }
      # 👇 關鍵修正
      - run: pip install requests feedparser google-generativeai yfinance
      - name: Run
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # 👇 關鍵修正：使用 FIN Token
          TG_BOT_TOKEN_FIN: ${{ secrets.TG_BOT_TOKEN_FIN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: python Daily_Cyber_Fin_v0.0.10.py --mode morning
      - name: Commit
        # 雖然這個 Bot 沒存檔，但為了保險起見保持一致邏輯
        run: |
          git config --global user.name "Action Bot"
          git config --global user.email "bot@github.com"
          git pull --rebase origin main
          # 這裡通常不需要 push，除非有改檔，保持這樣即可