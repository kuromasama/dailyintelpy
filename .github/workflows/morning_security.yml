
name: 🛡️ Morning Security
on:
  schedule:
    - cron: '0 0 * * *' # 台灣 08:00
    #- cron: '20 4 * * *' # 台灣 12:05 (測試用)
  workflow_dispatch:
jobs:
  run_job:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with: { python-version: '3.10' }
      # 👇 關鍵修正：一定要有 yfinance
      - run: pip install requests feedparser google-generativeai yfinance
      - name: Run
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # 👇 關鍵修正：使用 SEC Token
          TG_BOT_TOKEN_SEC: ${{ secrets.TG_BOT_TOKEN_SEC }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: python Daily_Cyber_Fin_v0.0.1.py --mode security
      - name: Commit
        run: |
          git config --global user.name "Action Bot"
          git config --global user.email "bot@github.com"
          git pull --rebase origin main
          git add security_log.md || true
          git commit -m "🛡️ Log Update" || echo "No changes"
          git push