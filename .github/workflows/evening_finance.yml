name: 💰 Evening Finance
on:
  schedule:
    - cron: '30 10 * * *' # 台灣 18:30
    #- cron: '20 4 * * *' # 台灣 12:05 (測試用)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run_job:
    runs-on: ubuntu-latest
    # 👇 加強權限宣告，確保 Job 也有寫入權限
    permissions:
      contents: write
      
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with: { python-version: '3.10' }
        
      - run: pip install requests feedparser google-generativeai yfinance
      
      - name: Run Finance Bot
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TG_BOT_TOKEN_FIN: ${{ secrets.TG_BOT_TOKEN_FIN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: python Daily_Cyber_Fin_v0.0.13.py --mode finance
        
      - name: Commit and Push
        run: |
          # 1. 設定身分
          git config --global user.name "Action Bot"
          git config --global user.email "bot@github.com"
          
          # 2. 加入所有產生的檔案 (包含 Log, JSON)
          git add portfolio.json *.log *.md
          
          # 3. 提交 (若無變更則略過，避免報錯)
          git commit -m "💰 Finance: Auto Update Logs & Portfolio" || echo "No changes to commit"
          
          # 4. 拉取最新變更 (使用 Rebase 防止分叉)
          git pull --rebase -X theirs origin HEAD
          
          # 5. 推送
          git push